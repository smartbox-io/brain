version: '2'
services:
  mongodb:
    image: mongo
  redis:
    image: redis
  brain:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/brain
    ports:
      - "3000:3000"
    depends_on:
      - mongodb
      - redis
